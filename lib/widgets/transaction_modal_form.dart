import 'package:flutter/widgets.dart';

class TransactionModalForm extends StatelessWidget {
  const TransactionModalForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
